import {el} from "redom"

export const main = el("main", { className:"main " +
    "d-flex flex-column align-items-center text-bg-light"})
export const section = el("section", { className:"hero-section " +
    "d-flex flex-column align-items-center text-bg-light"})
export const container = el("div", { className:"container " +
  "d-flex flex-column text-bg-light mt-5"})
export const title = el('h1','Payment form')


